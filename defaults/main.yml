cbsd_user: cbsd
cbsd_group: cbsd
cbsd_service: cbsdd
cbsd_flags: ""

cbsd_workdir: /usr/local/jails
cbsd_home: /usr/local/cbsd
cbsd_conf_file: "{{ cbsd_workdir }}/cbsd.conf"

cbsd_nodename: "{{ ansible_fqdn }}"
